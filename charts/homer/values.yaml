nameOverride:
namespaceOverride:
deployment:
  image:
    tag:
  env:
    port:
    initAssets:
  port:
  persistent:
    hostPath:
  nodeSelector: {}
service:
  type: ClusterIP
  loadBalancerIP:
  port:
  annotations: {}